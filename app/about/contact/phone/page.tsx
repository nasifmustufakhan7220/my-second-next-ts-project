import { Metadata } from "next";

export const metadata: Metadata ={
    title: "My contact phone"
}
const PhonePage = () => {
    return (
        <div>
            <h1>This is a contact phone number</h1>
        </div>
    );
};

export default PhonePage;